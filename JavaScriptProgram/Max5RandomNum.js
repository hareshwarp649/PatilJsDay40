
let v= Math.floor(Math.random()*900 +100);
let w= Math.floor(Math.random()*900 +100);
let x= Math.floor(Math.random()*900 +100);
let y= Math.floor(Math.random()*900 +100);
let z= Math.floor(Math.random()*900 +100);
if ([ $v >= $w && $v >= $x && $v >= $y && $v >= $z ])
{
	console.log("V is Maximum");
}
if ([ $v <= $w && $v <= $x && $v <= $y && $v <= $z ])
{
console.log("V is Minimum");

}
if ([ $w >= $v && $w >= $x && $w >= $y && $w >= $z ])
{
console.log("w is Maximum");
}
if ([ $w <= $v && $w <= $x && $w <= $y && $w <= $z ])
{
console.log("w is Minimum");
}
if ([ $x >= $v && $x >= $w && $x >= $y && $x >= $z ])
{
console.log("x is Maximum");
}
if ([ $x <= $v && $x <= $w && $x <= $y && $x <= $z ])
{
    console.log("x is Minimum");
}
if ([ $y >= $v && $y >= $w && $y >= $x && $y >= $z ])
{
    console.log("y is Maximum");
}
if ([ $y <= $v && $y <= $w && $y <= $x && $y <= $z ])
{
    console.log("y is Minimum");
 }
 if ([ $z >= $v && $z >= $w && $z >= $x && $y >= $y ])
 {
     console.log(" z is Maximum");
 }
if ([ $z <= $v && $z <= $w && $z <= $x && $z <= $y ])
{ 
    console.log("z is minimum");
}